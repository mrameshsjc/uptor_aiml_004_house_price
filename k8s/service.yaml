apiVersion: v1
kind: Service
metadata:
  name: house-price-api-service
spec:
  type: NodePort
  selector:
    app: house-price-api
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500
